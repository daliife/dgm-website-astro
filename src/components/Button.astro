---
import type { ButtonVariant, ButtonSize } from "../types/ui";
import { buildButtonClasses, isGlassVariant } from "../utils/buttonStyles";

interface Props {
  variant?: ButtonVariant;
  size?: ButtonSize;
  href?: string;
  target?: string;
  rel?: string;
  download?: string;
  className?: string;
  disabled?: boolean;
  loading?: boolean;
  onclick?: string;
  id?: string;
  "aria-label"?: string;
  "aria-expanded"?: string;
  "aria-controls"?: string;
  "data-collapse-toggle"?: string;
}

const {
  variant = "primary",
  size = "md",
  href,
  target,
  rel,
  download,
  className = "",
  disabled = false,
  loading = false,
  onclick,
  id,
  "aria-label": ariaLabel,
  "aria-expanded": ariaExpanded,
  "aria-controls": ariaControls,
  "data-collapse-toggle": dataCollapseToggle,
} = Astro.props;

const baseClasses = buildButtonClasses(variant, size, className);

const isLink = href !== undefined;
const Tag = isLink ? "a" : "button";

// Prepare common attributes
const commonAttrs: any = {};
if (id) commonAttrs.id = id;
if (onclick) commonAttrs.onclick = onclick;
if (ariaLabel) commonAttrs["aria-label"] = ariaLabel;
if (ariaExpanded) commonAttrs["aria-expanded"] = ariaExpanded;
if (ariaControls) commonAttrs["aria-controls"] = ariaControls;
if (dataCollapseToggle)
  commonAttrs["data-collapse-toggle"] = dataCollapseToggle;

const tagProps = isLink
  ? { href, target, rel, download, ...commonAttrs }
  : { disabled: disabled || loading, ...commonAttrs };
---

<Tag class={baseClasses} {...tagProps}>
  {
    isGlassVariant(variant) && (
      <div class="absolute inset-0 bg-gradient-to-br from-white/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 rounded-xl" />
    )
  }

  {
    loading && (
      <svg
        class="animate-spin h-4 w-4"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
      >
        <circle
          class="opacity-25"
          cx="12"
          cy="12"
          r="10"
          stroke="currentColor"
          stroke-width="4"
        />
        <path
          class="opacity-75"
          fill="currentColor"
          d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
        />
      </svg>
    )
  }
  <slot />
</Tag>

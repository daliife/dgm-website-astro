---
interface Props {
  variant?: "primary" | "secondary" | "outline" | "ghost";
  size?: "sm" | "md" | "lg";
  href?: string;
  target?: string;
  rel?: string;
  className?: string;
  disabled?: boolean;
  loading?: boolean;
}

const {
  variant = "primary",
  size = "md",
  href,
  target,
  rel,
  className = "",
  disabled = false,
  loading = false,
} = Astro.props;

const getVariantClasses = (variant: string) => {
  switch (variant) {
    case "primary":
      return "bg-gradient-to-br from-primary-600 via-secondary-600 to-primary-700 hover:from-primary-700 hover:via-secondary-700 hover:to-primary-800 text-bg-primary border-0 shadow-lg hover:shadow-2xl hover:shadow-primary-500/25 backdrop-blur-sm";
    case "secondary":
      return "bg-gradient-to-br from-primary-50/80 to-secondary-50/80 hover:from-primary-100/90 hover:to-secondary-100/90 text-primary-700 border border-primary-200/50 shadow-md hover:shadow-lg hover:shadow-primary-200/20 backdrop-blur-sm dark:from-primary-900/30 dark:to-secondary-900/30 dark:hover:from-primary-900/50 dark:hover:to-secondary-900/50 dark:text-primary-300 dark:border-primary-700/50";
    case "outline":
      return "bg-gradient-to-br from-transparent to-transparent hover:from-primary-50/60 hover:to-secondary-50/60 text-primary-600 border-2 border-primary-600 hover:border-primary-700 shadow-sm hover:shadow-md hover:shadow-primary-200/10 backdrop-blur-sm dark:text-primary-400 dark:border-primary-400 dark:hover:from-primary-900/20 dark:hover:to-secondary-900/20";
    case "ghost":
      return "bg-gradient-to-br from-transparent to-transparent hover:from-primary-50/40 hover:to-secondary-50/40 text-primary-600 border-0 shadow-sm hover:shadow-md hover:shadow-primary-200/5 backdrop-blur-sm dark:text-primary-400 dark:hover:from-primary-900/15 dark:hover:to-secondary-900/15";
    default:
      return "bg-gradient-to-br from-primary-600 via-secondary-600 to-primary-700 hover:from-primary-700 hover:via-secondary-700 hover:to-primary-800 text-bg-primary border-0 shadow-lg hover:shadow-2xl hover:shadow-primary-500/25 backdrop-blur-sm";
  }
};

const getSizeClasses = (size: string) => {
  switch (size) {
    case "sm":
      return "px-3 py-1.5 text-sm";
    case "md":
      return "px-4 py-2 text-base";
    case "lg":
      return "px-6 py-3 text-lg";
    default:
      return "px-4 py-2 text-base";
  }
};

const baseClasses = [
  "inline-flex",
  "items-center",
  "justify-center",
  "font-medium",
  "rounded-full",
  "border",
  "transition-all",
  "duration-500",
  "ease-out",
  "focus:outline-none",
  "focus:ring-2",
  "focus:ring-primary-500",
  "focus:ring-offset-2",
  "disabled:opacity-50",
  "disabled:cursor-not-allowed",
  "disabled:transform-none",
  "hover:transform",
  "hover:-translate-y-0.5",
  "active:transform",
  "active:translate-y-0",
  getVariantClasses(variant),
  getSizeClasses(size),
  className,
].join(" ");

const isLink = href !== undefined;
const Tag = isLink ? "a" : "button";
const tagProps = isLink
  ? { href, target, rel }
  : { disabled: disabled || loading };
---

<Tag class={baseClasses} {...tagProps}>
  {
    loading && (
      <svg
        class="animate-spin -ml-1 mr-2 h-4 w-4"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
      >
        <circle
          class="opacity-25"
          cx="12"
          cy="12"
          r="10"
          stroke="currentColor"
          stroke-width="4"
        />
        <path
          class="opacity-75"
          fill="currentColor"
          d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
        />
      </svg>
    )
  }
  <slot />
</Tag>

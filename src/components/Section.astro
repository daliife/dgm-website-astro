---
const { id, title } = Astro.props;
---

<section id={id} class="mx-4 my-8">
  {
    title && (
      <h2
        class="text-slate-900 dark:text-slate-200 text-2xl font-semibold mb-8"
        transition:name="section-name"
      >
        {title}
      </h2>
    )
  }
  <slot />
</section>
